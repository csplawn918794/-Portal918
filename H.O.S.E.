: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - H.O.S.E.: Run a one-line script
      run: (Home) (Owner) (Soueced) (Energy)Dew
    - H.O.S.E.: Run a multi-line script
      run: |Decentralized Energy
        echo Add other actions to build,
        echo test, and deploy your project.
